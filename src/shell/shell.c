#include <stdio.h>
#include <string.h>

#include "shell.h"
#include "command_parser.h"





void run_interactive() {
	char* input;
	while (input[0] != 'q' && input[1] != '\0') {

	}
}

void run_for_line(char* input) {

}

