#ifndef TEST_UTILS_SOURCE
#define TEST_UTILS_SOURCE

/// Redirect the stderr output to criterion
void redirect_stderr(void);

/// Redirect both stderr and stdout output to criterion
void redirect_all_std(void);

#endif
